#include<stdio.h>
int main()
{
 int base;
 printf("Enter the base of triangle:");
 scanf("%d",&base);
 int height;
 printf("Enter the height of triangle:");
 scanf("%d",&height);
 printf("Area of the triangle:%d",0.5*base*height);
 retun 0;
}
